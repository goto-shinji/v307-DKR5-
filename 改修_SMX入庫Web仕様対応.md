# SMX入庫 Web仕様対応

## 文書区分

| 項目 | 内容 |
|---|---|
| MD分類 | 改修 |
| 実施日 | 2026-06-21 |
| 対象 | BHTのSMX入庫、Socket通信、SQL Serverストアド |

## 調査内容

| 調査対象 | 確認結果 |
|---|---|
| BHTの従来SMX入庫 | カンマ区切りQRから親・子・数量を取得し、`SmxNyu`を1回送信していた |
| 既存共通電文 | `Soc.YmdSend`が呼出名、BHTID、BHT日付、BHT時間、社員CD、納入日を先頭に付加する |
| Socket応答 | `^`分割後、位置0が結果、位置1がメッセージ、位置2以降が追加OUTPUT |
| 通常QR | 品目マスタの洗浄方法を初期表示し、BHTで確定した入力値が0ならシートリーダへ登録する |
| `$Ny`仕様 | 6項目固定。洗浄方法の入力値にかかわらず、元レコードの終了日時更新後にシートリーダへ登録する |

## 改修内容

| 対象 | 内容 |
|---|---|
| `_Org/gSmxNyu.src` | `$Ny`の6項目解析、数値・必須チェック、品名照会、数量差異表示、登録分岐を追加 |
| `_Org/gSmxNyu.src` | 通常QRと`$Ny`に洗浄方法入力画面を追加し、QR値ではなくBHTの入力値を処理へ渡すよう修正 |
| `_Org/socZaiko.src` | `Soc.SmxNyu`を追加し、3通信で共通の個別項目順を使用 |
| `_Org/socZaiko.src` | `Soc.YmdSend`の例外復帰先を誤った`Soc.Send`側から自身の終了ラベルへ修正 |
| `_Org/socZaiko.src` | 電文作成後に存在した単独の`.`行を削除 |
| `ストアド/SmxNyuItem.sql` | 通常QRは品目マスタ、`$Ny`は元連番を照合して品名等を返すストアドを作成 |
| `ストアド/SmxNyuSheet.sql` | 通常QRの入力洗浄方法0、または終了更新後の`$Ny`を`T_シートリーダ`へ追加するストアドを作成 |
| `ストアド/SmxNyuFinish.sql` | `$Ny`の洗浄方法にかかわらず`T_SmxTrc.終了日時`を更新するストアドを作成 |
| `ストアド/SmxNyu*.sql` | 処理目的、入力順、検証、排他、再送、更新・登録、例外処理のコメントを追加 |
| `ソケットサーバ/ini/SmxNyuItem.ini` | 品名照会ストアドのINPUT／OUTPUT割当を追加 |
| `ソケットサーバ/ini/SmxNyuSheet.ini` | シートリーダ登録ストアドのINPUT／OUTPUT割当を追加 |
| `ソケットサーバ/ini/SmxNyuFinish.ini` | 終了日時更新ストアドのINPUT／OUTPUT割当を追加 |

## 通信仕様

| 呼出名 | 用途 | 正常時の主なOUTPUT |
|---|---|---|
| `SmxNyuItem` | 品名問い合わせ・登録前確認 | 結果、メッセージ、親品目、品名、元数量、マスタ洗浄方法 |
| `SmxNyuSheet` | `T_シートリーダ`登録 | 結果、メッセージ |
| `SmxNyuFinish` | `T_SmxTrc`終了日時更新 | 結果、メッセージ |

## 判断理由

| 判断 | 理由 |
|---|---|
| 3通信で同じINPUT形を使用 | Socketサーバー設定とBHT側送信処理を共通化し、項目ずれを防ぐため |
| 登録前にストアド側で再検証 | 品名照会後から登録までの間に他端末が処理する競合を防ぐため |
| 洗浄方法はBHT入力値で判断 | QR値やマスタ値は初期表示に限定し、作業者が確定した値を登録条件とするため |
| 通常QRの入力値0は`SmxNyuSheet` | 洗浄方法0でも`T_シートリーダ`へ実績を残すため |
| `$Ny`は終了更新後に必ずシート登録 | 元の`T_SmxTrc`を完了させたうえで、戻り品の入庫実績も必ず作成するため |
| 従来`SmxNyu`も残す | 通常QRの入力値が0以外の場合の既存運用を維持するため |

## 不具合対応

| 項目 | 内容 |
|---|---|
| 現象 | `Soc.YmdSend`で例外が発生すると、別関数の終了ラベルへ復帰する可能性があった |
| 原因 | エラーハンドラが`resume socSend.End`を指定していた |
| 影響範囲 | 山寺入庫、従来SMX入庫・出荷、新しいSMX入庫3通信 |
| 対応 | `resume socYmdSend.End`へ修正 |
| 回避方法 | 修正版を配布するまで、通信が安定した環境で使用し、異常時はDB登録結果を確認する |

### 洗浄方法0でシートリーダ実績が作成されない不具合

| 項目 | 内容 |
|---|---|
| 現象 | 通常QRで洗浄方法に0を入力しても`T_シートリーダ`にデータが作成されない |
| 原因 | 通常QRが新しい`SmxNyuSheet`を通らず、処理分岐もBHTの入力値を受け付けていなかった |
| 影響範囲 | SMX入庫で通常QRを読み取り、洗浄方法0として登録する端末・利用者 |
| 対応 | 洗浄方法入力画面を追加し、入力値0のとき`SmxNyuSheet`を呼ぶよう修正 |
| 回避方法 | 修正版配布までは対象QRの登録結果をDBで確認し、必要に応じて管理者が実績を補完する |

### `$Ny`でシートリーダ実績が作成されない不具合

| 項目 | 内容 |
|---|---|
| 現象 | `$Ny` QRで終了日時更新またはシートリーダ登録の一方しか実行されない |
| 原因 | 洗浄方法の値で`SmxNyuFinish`と`SmxNyuSheet`を排他的に分岐していた |
| 影響範囲 | `$Ny`戻り品QRを使用するSMX入庫端末・利用者 |
| 対応 | `SmxNyuFinish`成功後に`SmxNyuSheet`を連続実行するよう修正 |
| 回避方法 | 修正版配布までは`T_SmxTrc.終了日時`と`T_シートリーダ`の両方を確認する |

## 未対応・保留事項

| 区分 | 内容 |
|---|---|
| 未対応 | SQL Serverへのストアド適用 |
| 未対応 | 本番Socketサーバーへの3件のINI配置とサービス再起動 |
| 未対応 | BHT-BASIC開発環境でのコンパイル、実機・DB・Socketサーバー結合試験 |
| 保留 | 実DB列型と、`T_シートリーダ`の`tm`既定値が本仕様どおりかの最終確認 |
